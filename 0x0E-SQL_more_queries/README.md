# This repo contains sql queries on the following
	* Creating users
	* Granting permisions to users using GRANT
	* SQL join types which are
		** FULL JOIN
		** INNER JOIN
		** RIGHT JOIN
		** LEFT JOIN
		** NATURAL JOIN
